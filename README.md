# ai-age-fd

# 🧠 YouTube Video Semantic Timestamp Agent

This tool takes a YouTube video link and a prompt like:
> "Find when they talk about product features"

It returns the **timestamps** of relevant parts based on captions (no video download needed).

## 📦 Install

```bash
pip install -r requirements.txt
